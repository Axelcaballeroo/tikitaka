import type { Category, Provider } from "@/types";
export function CategoriesAdmin({ categories, providers }: { categories: Category[]; providers: Provider[] }) { return <><div><p className="text-xs font-extrabold uppercase tracking-[.16em] text-brand">Organización</p><h1 className="display mt-2 text-4xl font-semibold">Categorías</h1><p className="mt-3 text-sm text-muted">Categorías activas y oferta publicada en Supabase.</p></div><div className="mt-8 grid gap-4 md:grid-cols-2">{categories.map((category) => { const count = providers.filter((provider) => provider.categorySlug === category.slug).length; return <article key={category.slug} className="flex items-center gap-4 rounded-2xl bg-white p-5 shadow-sm"><span className={`grid h-12 w-12 shrink-0 place-items-center rounded-xl text-xl ${category.color}`}>{category.icon}</span><div className="min-w-0 flex-1"><h2 className="font-extrabold">{category.name}</h2><p className="mt-1 truncate text-xs text-muted">/{category.slug}</p></div><div className="text-right"><p className="display text-2xl font-semibold text-brand">{count}</p><p className="text-[11px] text-muted">proveedores</p></div><span className="h-2.5 w-2.5 rounded-full bg-emerald-400" title="Activa" /></article>; })}</div></>; }
