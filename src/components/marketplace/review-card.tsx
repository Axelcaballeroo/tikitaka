import Image from "next/image";
import type { ProviderReview } from "@/types";

export function ReviewCard({ review }: { review: ProviderReview }) { return <article className="rounded-[1.75rem] border border-teal-900/6 bg-white p-6 shadow-[0_12px_35px_rgba(20,78,72,.07)] transition duration-300 hover:-translate-y-1 hover:shadow-[0_18px_45px_rgba(20,78,72,.12)]"><div className="flex items-center justify-between gap-4"><div className="flex items-center gap-3"><Image src={review.avatar} alt={`Foto de ${review.author}`} width={48} height={48} className="h-12 w-12 rounded-full object-cover" /><div><p className="font-extrabold">{review.author}</p><time className="text-xs text-muted">{new Intl.DateTimeFormat("es-AR", { month: "long", year: "numeric" }).format(new Date(review.date))}</time></div></div><p aria-label={`${review.rating} estrellas`} className="whitespace-nowrap text-sm tracking-wide text-amber-500">{"★".repeat(review.rating)}<span className="text-slate-200">{"★".repeat(5 - review.rating)}</span></p></div><p className="mt-5 text-sm leading-7 text-muted">“{review.comment}”</p></article>; }
