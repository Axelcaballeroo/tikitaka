import { getContactEvents } from "@/lib/data/contact-events";
export const dynamic="force-dynamic";
export default async function AnalyticsPage(){const events=await getContactEvents();const byProvider=new Map<string,number>();const bySource=new Map<string,number>();events.forEach((event)=>{byProvider.set(event.providerName,(byProvider.get(event.providerName)??0)+1);bySource.set(event.source,(bySource.get(event.source)??0)+1)});const ranking=[...byProvider.entries()].sort((a,b)=>b[1]-a[1]);return <><p className="text-xs font-extrabold uppercase tracking-[.16em] text-brand">Contactos</p><h1 className="display mt-2 text-4xl font-semibold">Analytics de WhatsApp</h1><p className="mt-3 text-muted">Cada clic se registra sin interferir con la apertura de WhatsApp.</p><div className="mt-8 grid gap-5 lg:grid-cols-3"><article className="rounded-2xl bg-brand p-6 text-white"><p className="display text-5xl font-semibold">{events.length}</p><p className="mt-2 text-sm text-white/70">Contactos totales</p></article><section className="rounded-2xl bg-white p-6 shadow-sm"><h2 className="font-extrabold">Por fuente</h2><div className="mt-4 space-y-2">{[...bySource.entries()].sort((a,b)=>b[1]-a[1]).map(([source,count])=><p key={source} className="flex justify-between text-sm"><span>{source}</span><strong>{count}</strong></p>)}</div></section><section className="rounded-2xl bg-white p-6 shadow-sm"><h2 className="font-extrabold">Ranking</h2><div className="mt-4 space-y-2">{ranking.slice(0,5).map(([name,count],index)=><p key={name} className="flex justify-between text-sm"><span>{index+1}. {name}</span><strong>{count}</strong></p>)}</div></section></div><section className="mt-7 overflow-hidden rounded-2xl bg-white shadow-sm"><div className="border-b p-5"><h2 className="display text-2xl font-semibold">Últimos contactos</h2></div><div className="divide-y">{events.slice(0,50).map((event)=><div key={event.id} className="grid gap-2 p-4 text-sm sm:grid-cols-[1fr_120px_1fr_160px]"><strong>{event.providerName}</strong><span className="text-brand">{event.source}</span><span className="truncate text-muted">{event.page}</span><time className="text-xs text-muted">{new Intl.DateTimeFormat("es-AR",{dateStyle:"short",timeStyle:"short"}).format(new Date(event.createdAt))}</time></div>)}{!events.length&&<p className="p-8 text-center text-muted">Todavía no hay contactos registrados.</p>}</div></section></>}
