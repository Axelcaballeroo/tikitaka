import Link from "next/link";
import { requireAccount } from "@/lib/auth/account";
export default async function PublicViewPage() { const {provider}=await requireAccount();if(!provider)return <p>No encontramos tu perfil.</p>;return <><p className="text-xs font-extrabold uppercase tracking-[.16em] text-brand">Marketplace</p><h1 className="display mt-2 text-4xl font-semibold">Vista pública</h1>{provider.published?<div className="mt-8 rounded-[1.75rem] bg-mint p-8"><span className="rounded-full bg-white px-3 py-1 text-xs font-extrabold text-brand">Publicado</span><h2 className="display mt-5 text-3xl font-semibold">Tu perfil ya está visible</h2><p className="mt-3 text-muted">Las familias pueden encontrarte en el catálogo y contactarte por WhatsApp.</p><Link href={`/proveedores/${provider.slug}`} className="mt-6 inline-flex rounded-full bg-brand px-6 py-3 font-extrabold text-white">Ver mi perfil público ↗</Link></div>:<div className="mt-8 rounded-[1.75rem] border border-amber-200 bg-amber-50 p-8"><h2 className="display text-3xl font-semibold">Tu perfil todavía está en revisión</h2><p className="mt-3 leading-7 text-amber-900">Cuando el equipo de Tiki Taka lo apruebe, aparecerá en el marketplace.</p><Link href="/dashboard/perfil" className="mt-6 inline-flex rounded-full border-2 border-brand px-6 py-3 font-extrabold text-brand">Completar mi perfil</Link></div>}</>; }
